export const API_BASE_URL = 'https://api.dspice.co.uk/api';

export const API_URLS = {
  LOGIN: `${API_BASE_URL}/auth/login`,
  REGISTER: `${API_BASE_URL}/auth/register`,
  // Add other endpoints as needed
};
